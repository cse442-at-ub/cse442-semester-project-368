<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.a368">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.today.AddToSchedule"></activity>
        <activity
            android:name=".ui.appointment_meeting.createAppointment"
            android:label="@string/title_activity_create_appointment"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ui.friends.FriendProfileActivity"
            android:label="@string/title_activity_friend_profile"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ui.friends.AddFriendActivity"
            android:label="@string/title_activity_add_friend"
            android:theme="@style/AppTheme.NoActionBar" />
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/title_activity_login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>
    </application>

</manifest>